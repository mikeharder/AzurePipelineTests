pool:
  name: 'azsdk-pool-mms-ubuntu-2004-general'
  vmImage: 'MMSUbuntu20.04'

steps:
  - task: UsePythonVersion@0
    displayName: "Use Python 3.7"
    inputs:
      versionSpec: '3.7'
  
  - script: |
      which python
      which python3
      python3 -m venv env-perf

