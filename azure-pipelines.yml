pool:
  vmImage: 'ubuntu-16.04'

steps:
  - task: Npm@1
    inputs:
      command: 'install'
    displayName: 'npm install'

  - task: Npm@1
    inputs:
      command: 'custom'
      customCommand: 'test'
    displayName: 'npm test'
