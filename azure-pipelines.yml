variables:
  os.vmImage: 'ubuntu-18.04'

pool:
  vmImage: '$(os.vmImage)'

steps:
  - script: echo none

  - script: echo skipped
    condition: eq('foo', 'bar')

  - script: echo succeeded
    condition: succeeded()
