strategy:
  matrix:
    Linux:
      VmImage: 'ubuntu-16.04'
    Windows:
      VmImage: 'vs2017-win2016'

pool:
  vmImage: '$(VmImage)'

steps:
  - task: DotNetCoreCLI@2
    displayName: 'Run'
    inputs:
      command: run
