trigger: none

pr:
  branches:
    include:
      - main

strategy:
  matrix:
    1ESUbuntu2404:
      Pool: 'azsdk-pool'
      OSVmImage: 'ubuntu-24.04'
    1ESUbuntu2404Test:
      Pool: 'azsdk-pool-test'
      OSVmImage: 'ubuntu-24.04'
    1ESUbuntu2204:
      Pool: 'azsdk-pool-mms-ubuntu-2204-general'
      OSVmImage: 'ubuntu-22.04'

pool:
  name: $(Pool)
  vmImage: $(OSVmImage)

variables:
  skipComponentGovernanceDetection: true
  NugetSecurityAnalysisWarningLevel: 'none'
  system.debug: true

steps:
  - script: echo $(Agent.OS)
    displayName: print Agent.OS

  - script: uname -m
    displayName: uname -m

  - script: echo Linux
    condition: and(succeededOrFailed(), eq(variables['Agent.OS'], 'Linux'))
    displayName: Condition Linux

  - script: echo Windows
    condition: and(succeededOrFailed(), eq(variables['Agent.OS'], 'Windows_NT'))
    displayName: Condition Windows_NT

  - script: echo Mac
    condition: and(succeededOrFailed(), eq(variables['Agent.OS'], 'Darwin'))
    displayName: Condition Darwin

  - script: curl -v https://azuresdkforcpp.azurewebsites.net
    displayName: curl
