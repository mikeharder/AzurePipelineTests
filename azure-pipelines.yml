jobs:
  - job: Test

    strategy:
      matrix:
        Linux:
          OSVmImage: 'ubuntu-16.04'
        Windows:
          OSVmImage: 'vs2017-win2016'

    pool:
      vmImage: '$(OSVmImage)'

    steps:
      - script: set
