pool:
  vmImage: 'ubuntu-20.04'

steps:
  - pwsh: |
      foreach ($retry in 0..10) {
        Write-Host "attempt $retry"
        sleep 60
        if (!$LASTEXITCODE) { break }
      }
      exit $LASTEXITCODE
