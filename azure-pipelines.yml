jobs:
  - job:
    pool: server
    steps:
      - task: InvokeRESTAPI@1
        inputs:
          connectionType: 'connectedServiceName'
          serviceConnection: '$(ServiceConnection)'
          headers: ''
          method: 'GET'
          urlSuffix: 'get'
