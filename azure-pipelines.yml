strategy:
  matrix:
    Linux:
      os.name: 'Linux'
      os.vmImage: 'ubuntu-16.04'
    macOS:
      os.name: 'macOS'
      os.vmImage: 'macOS-10.13'
    Windows:
      os.name: 'Windows'
      os.vmImage: 'vs2017-win2016'

pool:
  vmImage: '$(os.vmImage)'

steps:
  - powershell: pwd
