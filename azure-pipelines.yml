variables:
  foo: 'bar'

strategy:
  maxParallel: $[ variables['foo'] ]
  matrix:
    Linux:
      VmImage: 'ubuntu-16.04'
    Windows:
      VmImage: 'vs2017-win2016'
    MacOs:
      VmImage: 'macOS-10.13'

pool:
  vmImage: '$(VmImage)'

steps:
  - script: echo hello
