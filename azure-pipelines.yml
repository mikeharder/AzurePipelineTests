strategy:
  matrix:
    Linux:
      os.vmImage: 'ubuntu-latest'
    Windows:
      os.vmImage: 'windows-latest'
    MacOs:
      os.vmImage: 'macOS-latest'

pool:
  vmImage: '$(os.vmImage)'

steps: 
  - bash: find .
