strategy:
  matrix:
    Linux:
      VM_IMAGE: 'ubuntu-20.04'
    Windows:
      VM_IMAGE: 'windows-2019'
    Mac:
      VM_IMAGE: 'macOS-10.15'

pool:
  vmImage: '$(VM_IMAGE)'

steps:
  - script: java -version
