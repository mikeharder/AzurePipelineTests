variables:
  os.vmImage: 'ubuntu-18.04'

pool:
  vmImage: '$(os.vmImage)'

steps:
  - script: echo none

  - script: echo skipped
    condition: and(succeeded(), eq('foo', 'bar'))

  - script: echo succeeded
    condition: and(succeeded(), eq('foo', foo))
