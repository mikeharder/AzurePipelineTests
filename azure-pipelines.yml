pool:
  vmImage: 'ubuntu-18.04'

steps:
  - script: |
      curl -v --location --request GET "$FR_URL" --header "$FR_KEY" --header 'Content-Type: application/json'
    env:
      FR_URL: $(fr-url)
      FR_KEY: $(fr-key)
