parameters:
  CustomCondition: true

steps:
  - script: |
      echo "parameters.CustomCondition: ${{ parameters.CustomCondition }}"

  - script: |
      echo "and(succeeded(), ${{ parameters.CustomCondition }})"
    condition: and(succeeded(), ${{ parameters.CustomCondition }})
